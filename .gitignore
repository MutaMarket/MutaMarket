/target
/storage
.env
node_modules
.DS_Store
/.svelte-kit

# Agent skill files the shadcn-svelte CLI drops into the frontend.
frontend/skills-lock.json

# Advertisement and gear creatives are the vendors' artwork: deployment
# storage, not the repository (the store sync downloads its own).
assets/img/ads/
assets/img/gear/
