/target
/storage
.env
node_modules
.DS_Store
/.svelte-kit

# Agent skill files the shadcn-svelte CLI drops into the frontend.
frontend/skills-lock.json

# Creatives the launcher-store sync downloads at runtime (numeric ids);
# the hand-made ones under assets/img/ads are committed.
assets/img/ads/[0-9]*.png
