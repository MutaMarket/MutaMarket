# The integration suites (tests/integration) share one process and one
# database; they clean the tables they assert about and a few mutate
# process env vars, so tests must never run concurrently.
[env]
RUST_TEST_THREADS = "1"
